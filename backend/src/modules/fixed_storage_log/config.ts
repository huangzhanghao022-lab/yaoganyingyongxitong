import { ModuleConfig } from '@cool-midway/core';

/**
 * Module config.
 */
export default () => {
  return {
    name: 'fixed-storage-log',
    description: 'Fixed storage update audit logs',
  } as ModuleConfig;
};
